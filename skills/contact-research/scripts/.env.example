# Copy this file to .env and fill in your real token — .env is gitignored
# and excluded from skill packaging, so it never leaves your machine.
#
# Usage: source .env && python3 hubspot_pull.py --run-dir state/ ...
export HUBSPOT_TOKEN=pat-na1-REPLACE_ME

# Only needed for api_batch_runner.py (unattended research via the Message Batches API).
export ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
